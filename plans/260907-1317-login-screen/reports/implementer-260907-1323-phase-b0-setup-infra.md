# Phase B0 — Setup: Playwright, Supabase SDK, next-intl

## Summary
Installed and wired the three infra pieces per the phase spec: `@playwright/test`
(E2E runner, chromium browser installed), `@supabase/ssr` + `@supabase/supabase-js`
(browser/server client factories), and `next-intl` in cookie-only mode
(`NEXT_LOCALE` cookie, no `[locale]` route segment). No screen UI/page logic touched.

## Files Touched
- `package.json` (+1 line: `test:e2e` script; deps/devDeps updated by npm install)
- `package-lock.json` (regenerated by npm)
- `.gitignore` (+4 lines: playwright artifact dirs)
- `playwright.config.ts` (new, +25 lines)
- `next.config.ts` (+3 lines: `next-intl/plugin` wiring)
- `src/lib/supabase/client.ts` (new, +19 lines)
- `src/lib/supabase/server.ts` (new, +37 lines)
- `.env.local.example` (new, +6 lines)
- `src/i18n/request.ts` (new, +27 lines)
- `src/messages/vi.json` (new, empty shell `{}`)
- `src/messages/en.json` (new, empty shell `{}`)
- `src/app/layout.tsx` (+9/-3 lines: wrapped in `NextIntlClientProvider`, locale from `getLocale()`)
- `e2e/.gitkeep` (new, placeholder so `testDir: "e2e"` exists)

## Checks
- `npx tsc --noEmit`: clean (after `npm run build` generated `.next/types`, required for the `LayoutProps` ambient type).
- `npm run build`: succeeds, compiles and generates static/dynamic routes for `/` and `/_not-found`.
- `npx eslint src next.config.ts playwright.config.ts`: clean.
- `npx playwright install --with-deps chromium`: completed (exit 0) — chromium binary installed.
- `npx playwright test`: runs, finds 0 tests (expected — no E2E specs written yet in this phase), confirms webServer/config wiring works end-to-end (spins up `npm run dev`, waits on `http://localhost:3000`, tears down cleanly). Exit code is 1 because Playwright treats "no tests found" as an error condition, not a run failure — this matches the phase's stated success criterion ("0 tests found is fine at this point").
- `npx jest src/app/page.test.tsx`: 2 passed — confirms the existing `/` route is unaffected.
- Full `npm test`: many pre-existing unrelated suites under `.claude/**` fail (jest worker crashes); this is a pre-existing repo-wide jest config issue unrelated to this phase's scope (jest.config.ts was untracked/pre-existing, not modified here) and out of scope for B0.

## Acceptance Criteria
- [x] `npm run build` and `npx tsc --noEmit` pass.
- [x] `npx playwright test` runs (0 tests found — runner wired correctly).
- [x] Existing `/` route still works (build succeeds, page test passes).

## Issues Encountered / Deviations
- None from the spec. One judgment call: per the out-of-scope note, `next-intl` was wired in cookie-only mode using `getRequestConfig` in `src/i18n/request.ts` (still required by `next-intl/plugin` even without `[locale]` routing) — this matches "no `getRequestConfig` route prefix" (i.e., no locale segment in the URL), not "don't use `getRequestConfig` at all," since the plugin requires it to resolve messages per request.
- Pre-existing, out-of-scope: root `npm test` fails on unrelated `.claude/**` jest suites (worker crashes) — flagging for visibility, not fixing here since jest.config.ts is outside this phase's file set.

**Status:** DONE
**Summary:** Playwright (+chromium), Supabase SSR client/server helpers, and next-intl (cookie-only, `NEXT_LOCALE`) are installed and wired; build, tsc, lint all clean; playwright test runner confirmed working with 0 specs; existing `/` route unaffected.
**Concerns/Blockers:** None blocking. Noted pre-existing unrelated jest failures under `.claude/**` for visibility (not in scope for B0).
